[project]
name = "rental-market-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "feedparser>=6.0.12",
    "python-dotenv>=1.2.2",
]
[tool.uv]
exclude-newer = "14 days"

[tool.uv.audit]
malware-check = true
